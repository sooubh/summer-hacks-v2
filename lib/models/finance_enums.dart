enum AccountType {
  bank,
  upi,
  cash,
}

enum TransactionType {
  income,
  expense,
  transfer,
  splitSettlement,
}

enum GoalStatus {
  active,
  paused,
  achieved,
}

enum InsightSeverity {
  info,
  warning,
  critical,
}

enum SplitStatus {
  pending,
  partial,
  settled,
}
